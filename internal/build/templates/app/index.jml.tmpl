_doctype page home

import component Layout from "components/layout"

Page {
    title: "Welcome to {{.ProjectName}}"
    description: "A modern web application built with JAWT"

    Layout {
        showWelcome: true
        projectName: "{{.ProjectName}}"
    }
}